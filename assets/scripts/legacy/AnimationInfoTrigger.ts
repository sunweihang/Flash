import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

/** Prefab stub — real logic lives in TikTokMiniGames; keeps build deserializing Enemy02. */
@ccclass('AnimationInfoTrigger')
export class AnimationInfoTrigger extends Component {
  InfoTrigger(_infoName: string): void {}
  Attack(): void {}
}
