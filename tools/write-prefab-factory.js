const fs = require("fs");
const content = "import {\r\n  AnimationClip,\r\n  JsonAsset,\r\n  Layers,\r\n  Material,\r\n  MeshRenderer,\r\n  Node,\r\n  ParticleSystem,\r\n  Prefab,\r\n  SkeletalAnimation,\r\n  SkinnedMeshRenderer,\r\n  assetManager,\r\n  instantiate,\r\n  resources,\r\n} from 'cc';\r\nimport { makeKnifeShard } from './NeonFactory';\r\n\r\ntype MonsterEntry = {\r\n  key: string;\r\n  unit: string;\r\n  fbxPrefabUuid: string | null;\r\n  resourcesPath: string;\r\n  flashPrefab?: string;\r\n  idleClipUuid?: string;\r\n};\r\n\r\ntype Catalog = {\r\n  materials: { monster: string; knife: string; effect: string };\r\n  monsters: MonsterEntry[];\r\n};\r\n\r\n/** Hardcoded zombie — survives catalog/json parse issues. */\r\nconst ZOMBIE: MonsterEntry = {\r\n  key: 'Enemy01',\r\n  unit: '2001',\r\n  resourcesPath: 'units/2001/Output/2001',\r\n  fbxPrefabUuid: '30bb0a00-acb7-440e-8ac5-ba5771785771@2b0a7',\r\n  flashPrefab: 'prefabs/Monster_Enemy01',\r\n  idleClipUuid: '308ee41d-7369-4228-82a5-7921a6c4b070@1f586',\r\n};\r\n\r\nconst HIDE_NAME_RE =\r\n  /RoleShadow|BloodHUD|BloodHUDBG|NeonAura|bone_hud|bone_hit|fire_point|HpBar|Shadow/i;\r\n\r\nlet _catalog: Catalog | null = null;\r\nlet _knifeMat: Material | null = null;\r\nlet _ready: Promise<void> | null = null;\r\n\r\nexport function warmupPrefabs(): Promise<void> {\r\n  if (_ready) return _ready;\r\n  _ready = (async () => {\r\n    try {\r\n      const json = await loadJson<Catalog>('prefabs/catalog');\r\n      if (json?.monsters?.length) _catalog = json;\r\n    } catch (err) {\r\n      console.warn('[PrefabFactory] catalog load failed, using builtin zombie', err);\r\n    }\r\n    if (!_catalog?.monsters?.length) {\r\n      _catalog = {\r\n        materials: {\r\n          monster: '9bd6d7a1-8842-4363-8d14-dd57a83c8efc',\r\n          knife: '2df8733a-d446-4e2d-9586-0f34a970382f',\r\n          effect: '402b29ed-02b9-4274-a1fd-f8a9f7d38a9f',\r\n        },\r\n        monsters: [ZOMBIE],\r\n      };\r\n    }\r\n    // Ensure zombie entry exists with FBX uuid.\r\n    const hasZombie = _catalog.monsters.some((m) => m.key === 'Enemy01' && m.fbxPrefabUuid);\r\n    if (!hasZombie) _catalog.monsters.unshift(ZOMBIE);\r\n\r\n    try {\r\n      _knifeMat = await loadByUuid<Material>(_catalog.materials.knife);\r\n    } catch (err) {\r\n      console.warn('[PrefabFactory] knife material missing', err);\r\n    }\r\n  })();\r\n  return _ready;\r\n}\r\n\r\nexport async function spawnMonsterPrefab(parent: Node, preferredKey?: string): Promise<Node> {\r\n  await warmupPrefabs();\r\n  const list = _catalog?.monsters?.length ? _catalog.monsters : [ZOMBIE];\r\n  const entry =\r\n    (preferredKey && list.find((m) => m.key === preferredKey)) ||\r\n    list.find((m) => m.key === 'Enemy01') ||\r\n    ZOMBIE;\r\n\r\n  const errors: string[] = [];\r\n  for (const candidate of uniqueEntries(entry, list)) {\r\n    try {\r\n      const model = await instantiateMonsterModel(candidate);\r\n      const skinned = model.getComponentInChildren(SkinnedMeshRenderer);\r\n      if (!skinned) {\r\n        model.destroy();\r\n        throw new Error(`no SkinnedMeshRenderer on ${candidate.key}`);\r\n      }\r\n      model.name = `Monster_${candidate.key}`;\r\n      parent.addChild(model);\r\n      try {\r\n        sanitizeRuntimeNode(model);\r\n        await playIdleSafe(model, candidate);\r\n        normalizeMonsterScale(model);\r\n      } catch (postErr) {\r\n        model.destroy();\r\n        throw postErr;\r\n      }\r\n      console.log('[PrefabFactory] spawned', candidate.key);\r\n      return model;\r\n    } catch (err) {\r\n      const msg = err instanceof Error ? err.message : String(err);\r\n      errors.push(`${candidate.key}: ${msg}`);\r\n      console.warn('[PrefabFactory] spawn failed', candidate.key, err);\r\n    }\r\n  }\r\n\r\n  // Last resort: raw zombie FBX only.\r\n  try {\r\n    const model = await loadZombieFbxDirect();\r\n    model.name = 'Monster_Enemy01';\r\n    parent.addChild(model);\r\n    sanitizeRuntimeNode(model);\r\n    await playIdleSafe(model, ZOMBIE);\r\n    normalizeMonsterScale(model);\r\n    console.log('[PrefabFactory] spawned via direct FBX');\r\n    return model;\r\n  } catch (err) {\r\n    console.error('[PrefabFactory] zombie FBX direct load failed', err, errors);\r\n  }\r\n\r\n  console.warn('[PrefabFactory] all monster prefabs failed, stick fallback', errors);\r\n  const { makeStickFigure } = await import('./NeonFactory');\r\n  return makeStickFigure(parent, 'Monster');\r\n}\r\n\r\nexport function spawnKnifePrefab(parent: Node, name = 'Knife'): Node {\r\n  const node = makeKnifeShard(parent, name, true);\r\n  if (_knifeMat) applyMaterialToRenderers(node, _knifeMat);\r\n  else {\r\n    void warmupPrefabs().then(() => {\r\n      if (_knifeMat) applyMaterialToRenderers(node, _knifeMat);\r\n    });\r\n  }\r\n  return node;\r\n}\r\n\r\nfunction uniqueEntries(first: MonsterEntry, list: MonsterEntry[]): MonsterEntry[] {\r\n  const out = [first];\r\n  for (const m of list) {\r\n    if (m.key !== first.key) out.push(m);\r\n  }\r\n  // Always try hardcoded zombie before giving up.\r\n  if (!out.some((m) => m.fbxPrefabUuid === ZOMBIE.fbxPrefabUuid)) out.push(ZOMBIE);\r\n  return out;\r\n}\r\n\r\nasync function instantiateMonsterModel(entry: MonsterEntry): Promise<Node> {\r\n  const attempts: Array<() => Promise<Node>> = [];\r\n\r\n  if (entry.fbxPrefabUuid) {\r\n    attempts.push(async () => {\r\n      const prefab = await loadByUuid<Prefab>(entry.fbxPrefabUuid!);\r\n      return instantiate(prefab);\r\n    });\r\n  }\r\n\r\n  // FBX path under resources (Cocos resolves imported scene prefab).\r\n  if (entry.key === 'Enemy01' || entry.unit === '2001') {\r\n    attempts.push(async () => {\r\n      const prefab = await loadResourcesPrefab('units/2001/Res/FBX/Demo_Zombie_01@skin');\r\n      return instantiate(prefab);\r\n    });\r\n  }\r\n\r\n  if (entry.resourcesPath) {\r\n    attempts.push(async () => {\r\n      const prefab = await loadResourcesPrefab(entry.resourcesPath);\r\n      return instantiate(prefab);\r\n    });\r\n  }\r\n\r\n  if (entry.flashPrefab) {\r\n    attempts.push(async () => {\r\n      const prefab = await loadResourcesPrefab(entry.flashPrefab!);\r\n      return instantiate(prefab);\r\n    });\r\n  }\r\n\r\n  const errors: string[] = [];\r\n  for (const tryLoad of attempts) {\r\n    try {\r\n      return await tryLoad();\r\n    } catch (err) {\r\n      errors.push(err instanceof Error ? err.message : String(err));\r\n    }\r\n  }\r\n  throw new Error(errors.join(' | ') || `no prefab source for ${entry.key}`);\r\n}\r\n\r\nasync function loadZombieFbxDirect(): Promise<Node> {\r\n  const prefab = await loadByUuid<Prefab>(ZOMBIE.fbxPrefabUuid!);\r\n  const node = instantiate(prefab);\r\n  if (!node.getComponentInChildren(SkinnedMeshRenderer)) {\r\n    node.destroy();\r\n    throw new Error('direct FBX has no SkinnedMeshRenderer');\r\n  }\r\n  return node;\r\n}\r\n\r\nfunction sanitizeRuntimeNode(root: Node): void {\r\n  const dangerous = [...root.components].filter((c) => {\r\n    const n = c.constructor?.name || '';\r\n    return /Enemy|Boss|Attachment|UnitActor|NavAgent|HpBar|Motor|Spawner|Missing|RigidBody|Collider/i.test(\r\n      n,\r\n    );\r\n  });\r\n  for (const c of dangerous) c.destroy();\r\n\r\n  hideHelperNodes(root);\r\n\r\n  try {\r\n    for (const ps of root.getComponentsInChildren(ParticleSystem)) ps.node.active = false;\r\n  } catch {\r\n    // particle module edge cases\r\n  }\r\n\r\n  for (const mr of root.getComponentsInChildren(MeshRenderer)) {\r\n    // SkinnedMeshRenderer extends MeshRenderer — never hide the body.\r\n    if (mr instanceof SkinnedMeshRenderer) continue;\r\n    if (mr.node.getComponent(SkinnedMeshRenderer)) continue;\r\n    const n = mr.node.name || '';\r\n    if (HIDE_NAME_RE.test(n) || /Quad/i.test(n)) mr.node.active = false;\r\n  }\r\n\r\n  for (const smr of root.getComponentsInChildren(SkinnedMeshRenderer)) {\r\n    smr.enabled = true;\r\n    smr.node.active = true;\r\n    let p: Node | null = smr.node;\r\n    while (p) {\r\n      p.layer = Layers.Enum.DEFAULT;\r\n      p.active = true;\r\n      if (p === root) break;\r\n      p = p.parent;\r\n    }\r\n  }\r\n  root.layer = Layers.Enum.DEFAULT;\r\n  root.active = true;\r\n}\r\n\r\nfunction hideHelperNodes(root: Node): void {\r\n  const stack: Node[] = [...root.children];\r\n  while (stack.length) {\r\n    const n = stack.pop()!;\r\n    if (HIDE_NAME_RE.test(n.name)) n.active = false;\r\n    for (const c of n.children) stack.push(c);\r\n  }\r\n}\r\n\r\nfunction normalizeMonsterScale(root: Node): void {\r\n  root.setScale(2.6, 2.6, 2.6);\r\n}\r\n\r\nfunction applyMaterialToRenderers(root: Node, mat: Material): void {\r\n  for (const mr of root.getComponentsInChildren(MeshRenderer)) {\r\n    if (mr instanceof SkinnedMeshRenderer) continue;\r\n    const count = Math.max(1, mr.sharedMaterials?.length || 1);\r\n    for (let i = 0; i < count; i++) mr.setSharedMaterial(mat, i);\r\n  }\r\n}\r\n\r\nasync function playIdleSafe(root: Node, entry: MonsterEntry): Promise<void> {\r\n  try {\r\n    let anim = root.getComponentInChildren(SkeletalAnimation);\r\n    if (!anim) {\r\n      anim = root.addComponent(SkeletalAnimation);\r\n    }\r\n\r\n    if (!anim.clips?.length && entry.idleClipUuid) {\r\n      try {\r\n        const clip = await loadByUuid<AnimationClip>(entry.idleClipUuid);\r\n        anim.clips = [clip];\r\n        anim.defaultClip = clip;\r\n      } catch (err) {\r\n        console.warn('[PrefabFactory] idle clip load failed', err);\r\n      }\r\n    }\r\n\r\n    if (!anim.clips?.length) return;\r\n    const idle = anim.clips.find((c) => c && /idle/i.test(c.name)) || anim.clips[0];\r\n    if (!idle) return;\r\n    anim.defaultClip = idle;\r\n    anim.play(idle.name);\r\n  } catch (err) {\r\n    console.warn('[PrefabFactory] playIdle skipped', err);\r\n  }\r\n}\r\n\r\nfunction loadResourcesPrefab(path: string): Promise<Prefab> {\r\n  return new Promise((resolve, reject) => {\r\n    resources.load(path, Prefab, (err, asset) => {\r\n      if (err || !asset) reject(err || new Error(`missing prefab ${path}`));\r\n      else resolve(asset);\r\n    });\r\n  });\r\n}\r\n\r\nfunction loadJson<T>(path: string): Promise<T> {\r\n  return new Promise((resolve, reject) => {\r\n    resources.load(path, JsonAsset, (err, asset) => {\r\n      if (err || !asset) {\r\n        reject(err || new Error(`missing json ${path}`));\r\n        return;\r\n      }\r\n      const data = (asset as JsonAsset).json;\r\n      if (!data || typeof data !== 'object') {\r\n        reject(new Error(`invalid json asset ${path}`));\r\n        return;\r\n      }\r\n      resolve(data as T);\r\n    });\r\n  });\r\n}\r\n\r\nfunction loadByUuid<T extends object>(id: string): Promise<T> {\r\n  return new Promise((resolve, reject) => {\r\n    assetManager.loadAny({ uuid: id }, (err, asset) => {\r\n      if (err || !asset) reject(err || new Error(`uuid ${id}`));\r\n      else resolve(asset as T);\r\n    });\r\n  });\r\n}\r\n";
fs.writeFileSync("d:/Custom/Flash/assets/scripts/game/PrefabFactory.ts", content);
console.log("ok", content.length);
